{
  "name": "gameplan-browser-agent",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Project 2 — ephemeral browser agent. Runs ONE CRM account per instance. Talks only to Redis: claims a job, scrapes, publishes results. Has no database driver and no credentials of its own.",
  "engines": {
    "node": ">=18.17"
  },
  "scripts": {
    "agent": "node agent.js",
    "start": "node agent.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "puppeteer-real-browser": "^1.3.18",
    "redis": "^4.7.0"
  }
}

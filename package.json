{
  "name": "game-plan-auto-browser",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Real-browser automation harness: launches Chrome via puppeteer-real-browser through a proxy pool, opens ConnectCDK Unify, follows the SSO redirect to the login page, then closes.",
  "engines": {
    "node": ">=18.17"
  },
  "scripts": {
    "start": "node index.js",
    "direct": "node index.js --direct",
    "headless": "node index.js --headless",
    "watch": "node index.js --keep-open 30"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "puppeteer-real-browser": "^1.3.18"
  }
}
